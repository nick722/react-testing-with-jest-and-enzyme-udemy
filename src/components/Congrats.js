import React from "react";

/**
 * Funcitonal react component for for congratulatory message.
 * @function
 * @returns {JSX.Element} - Rendered component (or null if 'success' prop is false)
 */
export default () => {
  return <div />;
};
